# TaskCube — 진행 현황

> 3D 칸반 업무 대시보드 (Vanilla JS · HTML · CSS, 빌드 도구 없음)
> 최종 업데이트: 2026-06-03

## 개요
- 업무를 **단계(X축) / 리스트(Y축) / 대분류(Z축)** 의 3차원으로 관리하는 칸반 보드
- **단계(X)**: 할 일 · 진행 중 · 검토 · 완료 등 워크플로우 컬럼
- **리스트(Y)**: 한 단계 안에서 위→아래로 쌓이는 카드 목록
- **대분류(Z)**: 집안일 · 회사일 등 프로젝트 단위 보드를 깊이(Z축)로 배치
- 데이터는 브라우저 `localStorage`에 저장(`taskcube.board.v1`)

## 기술 구성
- `index.html` — 마크업 · SVG 아이콘 스프라이트
- `styles.css` — 테마(다크/라이트) · 3D 씬 · 카드/컬럼/모달 스타일
- `app.js` — 상태관리 · 렌더 · 드래그앤드롭 · 뷰포인트 · CRUD · CSV/공유
- `.github/workflows/pages.yml` — GitHub Pages 자동 배포(+에셋 캐시버스팅)

## 배포
- **URL**: https://cheojang.github.io/workSpace/
- 브랜치 `claude/task-dashboard-drag-drop-XjKwS` 에 push하면 GitHub Actions가 자동 배포
- 배포 시 `styles.css`/`app.js`에 커밋 해시(`?v=SHA`)를 주입해 캐시 무효화 → F5 새로고침만으로 최신 반영

## 주요 기능
- **카드**: 추가/편집/삭제, 드래그로 단계 간 이동, 뒤집기(메모 확인·"다시 뒤집기" 버튼), 더블클릭 360° 스핀
- **단계(컬럼)**: 추가/이름변경/삭제, 제목을 카드형 헤더로 표시
- **대분류(Z축)**: 추가/이름변경/삭제, 상단 네비(`◀ 이름 n/N ▶ 🗑`)에서 휠/버튼/화살표 키로 **무한 순환 전환**, 활성 보드는 앞으로 강조
- **뷰포인트(입체 모드)**: 빈 공간 드래그로 시점 회전, **휠로 커서 중심 줌**, 더블클릭으로 시점·줌·위치 복귀, 하단 우측 X/Y 슬라이더
- **축 범례(gizmo)**: 좌측 상단에 시점 따라 회전하는 3축(단계/리스트/분류), 라벨은 빌보드(정면 유지)
- **평면 모드**: 활성 대분류를 2D로 펼쳐 보기
- **기타**: 검색(우측 상단), 다크/라이트 테마, CSV 내보내기/가져오기, 공유 링크 병합

## 조작 요약
| 동작 | 기능 |
|------|------|
| 카드 드래그 | 단계 간 이동 |
| 빈 공간 드래그 | 시점 회전 |
| 휠 | 커서 중심 줌 인/아웃 |
| 빈 공간 더블클릭 | 시점·줌·위치 복귀 |
| 상단 네비 휠 / ◀▶ / 화살표 키 | 대분류 전환(순환) |
| 카드 뒤집기 버튼 | 메모 확인 ↔ 복귀 |

## 작업 히스토리 (최신순)
1. 투명 gizmo + 빌보드 라벨, 헤더 네비 원복, 단계 제목 카드화
2. 시점 따라 회전하는 3D 축 gizmo, 동적 바닥 판
3. 3축 범례 추가(단계/리스트/분류)
4. 커서 중심 줌, 활성 보드 강조, 대분류 순환 전환
5. 검색 우측 이동, 카드 뒷면 "다시 뒤집기" 버튼
6. 빈 공간 드래그=회전 복원 + 휠 줌
7. 레이아웃 개편(상단 대분류 네비)
8. 버튼 명칭/발견성 정리(단계 추가 · 분류 추가)
9. 카드 드래그 견고화 + 배포 캐시버스팅
10. hover 기울기 제거, 시점 리셋, 모달 시인성 개선
11. **Z축을 대분류(프로젝트) 깊이로 재정의 + 카드 평면화** (핵심 구조 변경)
12. GitHub Pages 자동 배포 구성
13. 초기 큐브/스택 카드 실험 → 평면 전환
14. CSV 입출력 · 공유 링크
15. 최초 TaskCube 대시보드 구현

## 남은 아이디어 / 메모
- gizmo가 투명이라 좌상단 첫 컬럼과 겹쳐 보일 수 있음(시점 회전 시 해소)
- 대분류 간 카드 이동(현재는 활성 대분류 내에서만 이동)
- 모바일/터치 제스처 대응
